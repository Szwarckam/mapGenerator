import { FieldInfo } from "./interfaces";

const fieldInfo: FieldInfo = {
  clickedField: [],
  clickedItem: null,
  copied: [],
  history: [[]],
  paste: false,
  tempField: [],
};

export default fieldInfo;
