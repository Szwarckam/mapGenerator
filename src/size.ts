import { DivSize } from "./interfaces";
/**
 * Przetrzymuje rozmiary pola do rozstawiania i pola z przedmiotami.
 */
const divSize: DivSize = {
  fieldDivHeight: 16,
  fieldDivWidth: 24,
  itemsDivWidth: 8,
  itemsDivHeight: 16,
};

export default divSize;
